# Scripts

| Script | Qué hace | Sesión |
|---|---|---|
| `seed.py` | carga la línea base (`tests/fixtures/apu_linea_base.py`) en SQLite para reproducir el presupuesto completo | I0.4 |
| `meta_alpha.py` | corre la suite, ruff y el historial de git para reportar el estado de las 12 metas del sprint alpha (`OK`/`FALLA`/`PENDIENTE`) | sprint alpha |
