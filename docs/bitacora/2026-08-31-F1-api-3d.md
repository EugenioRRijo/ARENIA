# Bitácora — F.1 adelantada: API, simulador, UI multipágina y gestión 3D (I3.1, compuerta G1)

**Fechas:** 2026‑08‑31 · **Rama:** `main` (ocho incrementos `inc/F1-…` fusionados con `--no-ff`) ·
**Base:** `f9cae39` (tras la etiqueta `alpha-0.1`, con la spec y el plan de esta sesión ya
aprobados) · **Compuertas cruzadas:** **G1** (adaptador civil IFC, con salvedad — ver más abajo) ·
**Pendientes:** G0 (tutor), G2 (datos para `ml/`).

## Objetivo

Adelantar sobre el orden estricto de la cascada dos entregables de fases distintas del PLAN —
decisión aprobada por el usuario en conversación («enfoque B, aprobado tal cual»), con la spec
[`docs/superpowers/specs/2026-08-31-f1-api-simulador-design.md`](../superpowers/specs/2026-08-31-f1-api-simulador-design.md)
como autoridad de la sesión, por encima del orden del PLAN pero nunca de CLAUDE.md—: la **Sesión
F.1** completa (API FastAPI sobre el núcleo con esquema OpenAPI exportado, simulador determinista
de listas de precios para ejercitar UC‑02, UI Streamlit extendida a multipágina) y la **Sesión I3.1
/ compuerta G1** (generador programático de un modelo IFC de muestra, adaptador civil IFC, visor 3D
en la UI). Ambas sesiones estaban formalmente pendientes de compuertas propias — F.1 de G0
(documental, tutor) y I3.1 de G1 (IFC) — que esta sesión no sustituye: la API hereda lo que el
núcleo ya construyó en el sprint alpha sin tocar `core/`, y G1 se cruza aquí mismo con su propia
salvedad declarada (ver más abajo).

## Cómo se trabajó

Misma mecánica del sprint alpha: implementadores en paralelo por oleadas, cada uno en su propio
worktree y rama `inc/F1-…`, revisión independiente por tarea (cumplimiento de spec + calidad),
rondas de corrección acotadas y fusión a `main` por el controlador (`--no-ff`), que corrió la suite
completa al cierre de cada oleada. Oleadas: (T1, T3, T5) → (T2, T4) → (T6) → (T7) → (T8, este
cierre) — T2/T6 y T7/T8 se separaron de las oleadas previstas por el plan por los motivos que se
explican en *Decisiones* e *Impedimentos*. Los briefs, reportes, el paquete de revisión y el ledger
del sprint viven fuera de git (`.superpowers/sdd/2026-08-31-f1-api-3d/`); lo que sobrevive en el
repositorio es esta bitácora y el historial de commits.

## Qué se hizo, por tarea

| Tarea | Sesión del PLAN | Rama | Commits | Fusión | Reporte |
|---|---|---|---|---|---|
| T1 API base: aplicación, esquemas, sesión, catálogo y listas de precios | F.1 | `inc/F1-api-base` | `0ba9aab` | `9c4d02f` | [task-1](../../.superpowers/sdd/2026-08-31-f1-api-3d/task-1-report.md) |
| T2 Cómputos y presupuestos por HTTP (UC‑01/02/05) | F.1 | `inc/F1-api-presupuestos` | `e728915`, `4aae8a7` | `471df04` | [task-2](../../.superpowers/sdd/2026-08-31-f1-api-3d/task-2-report.md) |
| T3 Simulador determinista de listas de precios | F.1 | `inc/F1-simulador` | `d1255cb` | `52f4129` | [task-3](../../.superpowers/sdd/2026-08-31-f1-api-3d/task-3-report.md) |
| T4 UI multipágina (catálogo, elaborar, histórico, simulador) | F.1 | `inc/F1-ui` | `9b4b9db`, `753d773` | `ddc6d3d` | [task-4](../../.superpowers/sdd/2026-08-31-f1-api-3d/task-4-report.md) |
| T5 Generador programático de `tanquilla.ifc` | I3.1 | `inc/F1-ifc-muestra` | `7f162ad` | `5948508` | [task-5](../../.superpowers/sdd/2026-08-31-f1-api-3d/task-5-report.md) |
| T6 Adaptador civil IFC + entrada `.ifc` en la API (compuerta G1) | I3.1 | `inc/F1-adaptador-ifc` | `dede662`, `d8a1423` | `c8e4b79` | [task-6](../../.superpowers/sdd/2026-08-31-f1-api-3d/task-6-report.md) |
| T7 Visor 3D en la UI | I3.1 | `inc/F1-visor` | `9f654b7`, `f7de199` | `46e87ae` | [task-7](../../.superpowers/sdd/2026-08-31-f1-api-3d/task-7-report.md) |
| T8 OpenAPI a `docs/api.json`, bitácora e integración | F.1 | `inc/F1-openapi` | (este cierre) | — | esta bitácora |

Entre la fusión de T1 y el despacho de T2, el controlador aplicó directamente sobre `main` el commit
`904fc64` (`build(deps): httpx2 en el grupo dev para el testclient de la api`, ver *Decisiones*), no
atribuible a ningún brief de tarea.

Momento del trabajo alcanzado (CLAUDE.md §1): compuerta **G1 cruzada** — ver la sección dedicada más
abajo.

## Compuerta G1 — CRUZADA

`AdaptadorCivilIFC.extraer("data/samples/tanquilla.ifc")` (`adapters/civil/ifc.py`) reproduce
**exactamente** el cálculo manual de la línea base: **0,224 m³** por tanquilla, la misma cifra que
`tests/unit/test_reglas_civil.py` y que CLAUDE.md §4/§8.2 exigen como criterio de la compuerta. El
extractor localiza la cantidad por su **tipo IFC** (`IfcQuantityVolume` para `m3`,
`IfcQuantityArea`/`IfcQuantityLength`/`IfcQuantityCount` para `m2`/`m`/`pieza`), no por el nombre de
la propiedad ni del `Qto_*` que la contiene — no da por hecho que el productor del modelo llame
"Qto_APU" a su conjunto de cantidades.

**Salvedad declarada.** `data/samples/tanquilla.ifc` es un modelo **generado programáticamente**
por `scripts/generar_tanquilla_ifc.py` (Tarea 5, vía `ifcopenshell.api`), no un export real de una
herramienta BIM (Revit, Bonsai). El extractor en sí no distingue el productor del archivo — lee
`Pset_APU` y el `IfcElementQuantity` asociado por tipo, sin ninguna suposición sobre el origen de la
geometría —, pero la compuerta **no se ha ejercitado todavía contra un IFC exportado por una
herramienta BIM real**: algunos exportadores representan cantidades como `IfcPropertySingleValue`
numérico en vez de con el tipo `IfcQuantity*` explícito que este adaptador busca, caso en el que
fallaría de forma ruidosa (`ValueError` con archivo y `GlobalId`) en vez de extraer un valor
incorrecto — comportamiento intencional, pero es la brecha a vigilar cuando llegue un IFC real
(CLAUDE.md §8.2: si algún día no coincidiera, el adaptador tabular sigue siendo la entrada civil sin
que el resto del sistema cambie).

Este es un cambio de estado respecto de la bitácora previa del sprint
([`2026-08-31-alpha-fixes.md`](2026-08-31-alpha-fixes.md), que registraba «G0, G1 y G2 siguen
abiertas»): esta entrada deja constancia de que G1 pasó a **cruzada, con la salvedad anterior como
condición**.

## Decisiones

| Decisión | Motivo |
|---|---|
| Desviación de la cascada: F.1 e I3.1 se adelantan sobre el orden del PLAN (enfoque B) | Aprobación explícita del usuario en conversación; la spec de esta sesión queda como autoridad, sin sustituir las compuertas G0/G1 propias de cada sesión — G0 sigue pendiente del tutor, G1 se cruza en esta misma sesión con su salvedad |
| Los montos viajan como texto `Decimal` en el JSON, en ambas direcciones | Regla 3 de CLAUDE.md; ahora declarado explícitamente en la descripción de la aplicación FastAPI (`api/main.py`, ver *Hallazgos* — no estaba en T1) |
| `httpx2` en el grupo dev de `pyproject.toml`, en vez de `httpx` clásico | `starlette` 1.6 deprecia `TestClient` con `httpx` clásico; la advertencia rompía el DoD de cero advertencias. `httpx2` ya resolvía en `uv.lock`; el commit `904fc64` lo declara sin agregar una dependencia nueva al proyecto |
| Pset del modelo IFC de muestra deliberadamente distinto de la descripción del fixture de la línea base | El vínculo entre el modelo IFC y el sistema es el **código de partida** (`COVENIN_Codigo="LB-04-CON"`), no el texto de `Partida_Descripcion`; ratificado por el controlador en T5 y no contradicho por ningún test de T6 aguas abajo — ni la compuerta G1 ni R4 comparan la descripción literal |
| `three.js` r128 (CDN cdnjs) con autorrotación en vez de `OrbitControls` | `OrbitControls.js` no está publicado en cdnjs para ninguna versión disponible (404 verificado con `curl`); `r128` sí resuelve (200, contenido real) — la rotación automática sobre el centro geométrico del modelo es la alternativa que la spec permite explícitamente |
| Imports perezosos de `ifcopenshell` en `ui/visor3d.py` y `ui/paginas/visor.py` | Los extras del proyecto son independientes (`ui`, `api`, `civil`); un import de `ifcopenshell` a nivel de módulo en una página registrada incondicionalmente por `ui/app.py` habría hecho que **toda** la UI (no solo el visor) dejara de poder importarse sin el extra `civil` instalado — hallazgo *Important* de la revisión de T7, corregido en su ronda de fixes |
| Corrección de la aserción imposible del Paso 1 del brief de T3 | El texto original del plan comparaba una lista plana contra listas anidadas con corchetes de más y no podía satisfacerse nunca; la corrección preserva la intención de la spec §5 («respeto de `--insumos`») y fue aceptada en la revisión de esa tarea |
| T6 y T7 pasaron a oleadas propias, no en paralelo como preveía el plan original | T2 y T6 comparten `api/rutas/computos.py` y `tests/integration/test_api.py` (conflicto detectado en el *pre-flight scan*); T7 y T8 se declararon secuenciales porque la bitácora que escribe T8 debe documentar la verificación manual de T7 — costo: dos oleadas más de *wall-clock* |

## Hallazgos

1. **La descripción de la aplicación FastAPI no declaraba el contrato de montos como texto.** El
   brief de esta tarea (Paso 2) pedía confirmarlo; T1 había dejado `FastAPI(title=..., version=...)`
   sin `description`. Se agregó en `api/main.py` (dentro del alcance de esta tarea, según autorizó
   su propio despacho) un párrafo que declara explícitamente que todo monto viaja como `str` en
   formato decimal y se convierte con `Decimal(texto)` en la frontera — visible ahora en
   `docs/api.json` (`info.description`) y en Swagger UI (`/docs`).
2. **`uv sync --extra ui --extra api --extra civil` en un entorno limpio no deja `httpx` clásico
   instalado, y eso es correcto.** T1 lo había resuelto localmente sin tocar `pyproject.toml`; el
   controlador cerró la duda declarando `httpx2` en el grupo dev (`904fc64`). Confirmado de nuevo en
   este worktree: el entorno arranca con `httpx2` 2.12.0 (sin `httpx` clásico) y
   `fastapi.testclient.TestClient` funciona sin ninguna advertencia.
3. **Hallazgos menores diferidos a lo largo del sprint** (no bloquean el cierre, quedan para una
   sesión futura): rama 409 de `/presupuestos/{codigo}/actualizacion` sin prueba dedicada cuando
   ningún insumo cambia de precio; `Decimal()` malformado da `InvalidOperation` sin manejador
   propio (500 crudo); `ImportError` → 400 del adaptador civil IFC sin evidencia RED real (no se
   pudo desinstalar el extra del entorno compartido sin arriesgar tareas en paralelo); ambigüedad de
   dos `IfcQuantityVolume` del mismo tipo en un elemento, sin fixture que la ejercite; consulta de
   histórico de cambios de precio duplicada entre `ui/paginas/historico.py` y
   `api/rutas/listas.py` (candidata a consolidar en `core.catalog`).

## Estado de la suite y de la meta al cierre de esta tarea

```
uv run pytest -W error        -> 330 passed, 0 advertencias
uv run ruff check .           -> All checks passed!
uv run ruff format --check .  -> 113 files already formatted
uv run python scripts/meta_alpha.py -> 12/12 metas OK (330 pruebas; cobertura de core/ 97,90 %;
                                        M9: 9 commits evaluados, 5 ramas de adaptador con
                                        git diff core/ vacio)
```

## Impedimentos

- **Dos muertes de sesión del controlador durante el sprint.** La primera, con la oleada 1 (T1, T3,
  T5) en vuelo: la sesión murió sin commits ni reportes, solo archivos sueltos sin commitear en los
  worktrees. Se limpiaron con `git clean -f` (todo regenerable: el código completo seguía en los
  briefs) y la oleada se re‑despachó desde cero — costo, rehacer del orden de 50 líneas de
  transcripción. La segunda, tras despachar el revisor de T6: el *merge* de T6 a `main` (`c8e4b79`)
  sí sobrevivió en git, pero el veredicto de esa revisión no quedó registrado en el ledger, así que
  se re‑despachó sobre el mismo paquete ya fusionado (`review-ddc6d3d..d8a1423.diff`). En ningún
  caso se perdió un commit: la disciplina de fusionar cada tarea a `main` de inmediato y de escribir
  los reportes en el workspace central (`.superpowers/sdd/`, fuera de los worktrees efímeros)
  permitió recuperar el estado sin rehacer trabajo ya integrado.
- **Conflicto de archivos T2 ↔ T6** (ambos tocan `api/rutas/computos.py` y
  `tests/integration/test_api.py`), detectado en el *pre-flight scan* antes de despachar: obligó a
  mover T6 a su propia oleada en vez de ejecutarse en paralelo con T2 — una oleada más de
  *wall-clock*, sin conflicto de fusión real.
- **Tope de dos *worktrees* creados por mensaje de despacho** (carrera conocida del harness,
  observada ya en el sprint alpha): obligó a escalonar la oleada 1 en dos despachos separados.

## Próximos pasos

1. Presentar la documentación de Fase 0 al tutor (G0), pendiente desde el sprint alpha.
2. Ejercitar la compuerta G1 contra un IFC exportado por una herramienta BIM real (Revit o Bonsai),
   para retirar la salvedad de «modelo generado programáticamente» declarada en esta bitácora.
3. Contar registros de APU por dominio y decidir la técnica de `ml/prediction/` (G2, CLAUDE.md §8.1).
4. Atender los hallazgos menores diferidos (sección *Hallazgos* de esta bitácora y de los reportes
   de cada tarea en `.superpowers/sdd/2026-08-31-f1-api-3d/`).
