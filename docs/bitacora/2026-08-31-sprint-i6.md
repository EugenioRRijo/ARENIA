# Bitácora — Sprint I6: rendimientos auditables y predicción (Sesiones I6.2 e I6.3)

**Fecha:** 2026‑08‑31 · **Espacio:** worktree `sprint-i6` (pedido del usuario), rama de
integración `worktree-sprint-i6`, una rama por sesión fusionada `--no-ff` · **Base:** etiqueta
`i6-base` (= `2502c51`, merge de I6.1) · **Meta:** `scripts/meta_i6.py`, **definida y commiteada
antes de implementar** (principio 5 de CLAUDE.md) y cerrada en **12/12** · **Compuertas:**
**G2 CRUZADA** (ver abajo). Pendientes: G0 (tutor) y la salvedad de G1 (IFC real).

## Mecánica

A pedido del usuario, el sprint corrió con dos novedades respecto de las sesiones anteriores:

1. **Worktree aislado** (`.claude/worktrees/sprint-i6`): `main` intacto hasta la fusión final.
2. **Meta del sprint primero**: `scripts/meta_i6.py` define las 12 metas reutilizando la
   maquinaria de `meta_alpha.py` (parametrizada en `_evaluar(..., metas=...)`, un solo lugar,
   DRY). La línea base del sprint fue 2/12 (solo ruff y suite); el cierre exige 12/12, y las
   metas de pruebas nacieron PENDIENTE porque sus archivos no existían — el sprint se escribió
   para ponerlas en OK, no al revés.

TDD estricto en ambas sesiones: cada módulo tuvo su RED observado (ImportError o aserciones
fallando) antes de una línea de implementación.

## Sesión I6.2 — Rendimientos auditables (UC‑06) — commits `feat(core)` y `feat(api)`

- **`core/catalog/rendimientos.py`**: `registrar_ejecucion` (obra real, referencia única),
  `registrar_rendimiento` (el contrato valida primero: valor > 0, MEDIDO exige referencia, y la
  referencia debe apuntar a una `Ejecucion` registrada — trazabilidad total), `dispersion_rendimientos`
  (n, media, mínimo, máximo, desglose estimado/medido, todo `Decimal`), `proponer_rendimiento`
  (el MEDIDO más reciente pesa más que lo declarado; el tipo siempre a la vista) y
  `advertencia_rendimiento` (RF‑27: rango observado, con abstención bajo 2 observaciones).
  Sin `commit` en el catálogo: la transacción es de la capa que decide.
- **`api/rutas/rendimientos.py`**: `/ejecuciones` y `/partidas/{codigo}/rendimientos` (GET con
  histórico + dispersión + propuesta; POST que registra, confirma y devuelve la advertencia
  **sin impedir jamás** — 201 siempre que la petición sea válida). La advertencia normativa es la
  de rango; con el extra `ml` instalado se enriquece con el veredicto del Isolation Forest de
  I6.1 (import perezoso). `docs/api.json` regenerado.
- El modelo de datos **ya contemplaba** UC‑06 (`Ejecucion`, `ck_rendimiento_medido_exige_ejecucion`
  desde I0.4): esta sesión no tocó `core/models` ni los contratos.

## Sesión I6.3 — Predicción de precio (UC‑07) — commit `feat(ml)`

- **Compuerta G2 CRUZADA**: `tecnica_para` es la tabla de CLAUDE.md §8.1 hecha código; el conteo
  real sobre el catálogo sembrado (civil = 5, telecom/industrial/sistemas = 0, todos < 50) dicta
  **sistema de reglas con análisis de sensibilidad, declarado como limitación**. La evidencia
  vive en `docs/resultados_ml.md` (generado, reproducible) y en `tests/unit/test_prediccion.py`.
- **`ml/prediction/reglas.py`**: regla declarada (PU estimado = PU base × (1 + media de las
  variaciones del histórico)), rango de sensibilidad (variación mínima y máxima), métricas
  MAPE/RMSE/R² en `Decimal` (raíz con `Decimal.sqrt()`), y `contrastar_aace` (RF‑29): `Hallazgo`
  de severidad ADVERTENCIA del contrato cuando la desviación sale de la clase 3 de AACE
  (−20 % / +30 %, límites inclusivos, RP 18R‑97).
- **`scripts/generar_resultados_ml.py`** evalúa el caso UC‑02 de la línea base sobre una base en
  memoria — el motor de costos es la verdad de terreno — y escribe `docs/resultados_ml.md`:
  **MAPE 12,46 %, RMSE 5,10 USD, R² 0,9974, 0 hallazgos AACE**. El MAPE alto es la limitación en
  acción: la regla usa un índice agregado y no conoce la composición; el documento lo declara.

## Decisiones

| Decisión | Motivo |
|---|---|
| Meta del sprint antes que el código, reutilizando `meta_alpha._evaluar` parametrizado | Principio 5 de CLAUDE.md aplicado al nivel de sprint; DRY: una sola maquinaria de evaluación |
| La advertencia normativa de RF‑27 es la de **rango observado** (core); el veredicto del bosque es enriquecimiento opcional (API, import perezoso) | El criterio del ERS habla de «rango observado»; `core` no puede importar `ml` (arquitectura), y sin el extra `ml` la API debe funcionar igual |
| `MINIMO_OBSERVADO_PARA_ADVERTIR = 2` en el rango (vs 8 del bosque) | Un punto no define comportamiento; dos ya dan un rango real. El bosque exige más porque es un juicio estadístico |
| La regla de predicción NO conoce la composición del APU | Si la conociera, recalcularía lo mismo que `core.costing` (circular, error cero, métrica vacía); su error frente al motor es exactamente lo que las métricas miden |
| R² presentado a 4 decimales en el informe | A dos, 0,9974 se presenta «1.00»: engaña |
| `scripts/generar_resultados_ml.py` importa `tests.fixtures` | Precedente de `scripts/seed.py`: la línea base vive una sola vez (CLAUDE.md §2, DRY) |

## Hallazgos

1. **UC‑06 en la UI queda diferido**: «al crear un APU, proponer el rendimiento» presupone un
   flujo de composición manual de APU que ninguna pantalla ofrece todavía — la misma familia del
   hallazgo RF‑16 (I2). La API es el front del caso de uso (Swagger, decisión F.1); la página
   llegará con el flujo de creación de partidas/APU (G0 o Fase 6).
2. **`EnterWorktree` falló al crear por un problema de mayúsculas de ruta en Windows** (`c:` vs
   `C:`): el worktree quedó creado y la entrada funcionó por `path`. Anotado por si reaparece.
3. La escala de `decision_function` dependiente del tamaño de muestra (hallazgo de I6.1) no
   estorbó aquí: el enriquecimiento del bosque en la API se abstiene con menos de 8 observaciones.

## Estado al cierre (evidencia de `scripts/meta_i6.py`)

```
RESULTADO: 12/12 metas OK
suite: 385 pruebas, 0 fallidas, 0 errores (con -W error)
cobertura core/: 97,99 % (umbral 80)
M9: ningun commit mezcla core/ con adapters/ o ml/; rama de ml con git diff core/ vacio
ruff check y ruff format --check limpios
```

## Próximos pasos

1. G0 (tutor): ahora con dos hallazgos acumulados de la familia «flujo de creación» (RF‑16 e
   ítem 1 de esta bitácora) y la compuerta G2 cruzada para validar.
2. La salvedad de G1: ejercitar el adaptador IFC contra un export real (Revit/Bonsai).
3. Fase 6 restante: F.2 (plan de pruebas y calidad) y F.3 (manuales).
