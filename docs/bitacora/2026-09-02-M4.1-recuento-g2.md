# Bitácora — Sesión M4.1 (recuento G2 y resultados ML multidominio)

**Fecha:** 2026‑09‑14 (el archivo conserva el prefijo `2026-09-02` de la serie del sprint, como
fija el plan) · **Rama:** `worktree-sprint-multidominio` · **Base:** `6c3163c`
(`feat(sistemas): presupuesto por puntos de funcion con tarifas del tabulador`, cierre de M3.2) ·
**Compuerta:** **GM4 CRUZADA** (informativa)

## Objetivo

Regenerar el conteo de la compuerta G2 con los cuatro dominios poblados y
`docs/resultados_ml.md` con métricas por dominio donde exista un histórico real de variaciones
(PLAN_MULTIDOMINIO, Sesión M4.1). G2 no se revierte: la técnica sigue siendo reglas y la
limitación sigue declarada, ahora con catálogos reales en vez de ausencia de datos.

**Resultado:** conteos civil 5 · telecom 40 · industrial 4 · sistemas 9 (todos < 50 → reglas);
telecom evaluado sobre su histórico real (MAPE 27,83 %); industrial y sistemas declarados no
evaluables. Suite 479 en verde, `core/` intacto, meta 12/12.

## Continuidad

La sesión se retomó a medias. Una sesión anterior dejó **sin commitear y sin bitácora** la
extensión de `scripts/generar_resultados_ml.py`, el informe regenerado y
`tests/unit/test_resultados_ml_multidominio.py` (4 pruebas); no hay registro de la fase RED de
esas cuatro pruebas. Antes de continuar se verificó el punto de partida:

1. `docs/resultados_ml.md` en disco era, byte a byte, la salida del script (reproducible).
2. Suite completa verde con `-W error` (478 pruebas) y meta 12/12.

Después se revisó el código pendiente como si fuera un cambio ajeno; las correcciones están abajo.

## TDD (corrección de esta retomada)

1. **RED.** `test_contraste_aace_presenta_a_dos_decimales_con_redondeo_hacia_arriba`
   (`tests/unit/test_prediccion.py`, prueba nueva; ninguna existente se modificó) falló con
   `assert '(2.13)' in 'el precio construido de X (2.12) …'`.
2. **GREEN.** `_a_dos_decimales` en `ml/prediction/reglas.py` (cuantiza con `ROUND_HALF_UP`);
   9/9 en `test_prediccion.py`.

## Conteo G2: antes y después

| Dominio | I6.3 (`m-base`) | M4.1 | Técnica (`tecnica_para`) |
|---|---|---|---|
| civil | 5 | 5 | reglas |
| telecom | 0 | 40 | reglas |
| industrial | 0 | 4 | reglas |
| sistemas | 0 | 9 | reglas |

Registros = partidas del catálogo sembrado por dominio, con los cuatro seeds sobre una misma base
en memoria (`conteo_multidominio`).

## Métricas por dominio

| Dominio | Histórico | Partidas | Cambios | MAPE | RMSE | R² | AACE |
|---|---|---|---|---|---|---|---|
| civil | lista base → muestra 01/06/2026 (I6.3) | 5 | — | 12,46 % | 5,10 USD | 0,9974 | 0 |
| telecom | ARENAZA 18/05/2026 → MaPreX 09/07/2026 (M1.3) | 40 | 5 | 27,83 % | 28,16 USD | 0,8866 | 38 |
| industrial | una sola lista real (M2.2) | — | — | no evaluable | — | — | — |
| sistemas | una sola lista real (M3.2) | — | — | no evaluable | — | — | — |

Civil no cambia respecto de I6.3. Telecom: los cinco `CambioPrecio` mueven el PU de 8 de las 40
partidas.

## Qué se cambió

| Archivo | Cambio |
|---|---|
| `scripts/generar_resultados_ml.py` | Tres acopios, cada uno en su base en memoria: `conteo_multidominio`, `evaluar_sobre_linea_base` (I6.3) y `evaluar_sobre_telecom`; `EvaluacionDominio`; `generar_informe(…, por_dominio=…)` sigue siendo pura. En la retomada: tipos en `_en_base_nueva` y `precio_con`; nombre de la lista base sin la fecha duplicada; se retiró «(mismo patrón)» del resumen de hallazgos |
| `docs/resultados_ml.md` | Regenerado por el script (no a mano) |
| `ml/prediction/reglas.py` | La descripción del hallazgo AACE presenta a dos decimales con `ROUND_HALF_UP` |
| `scripts/derivar_listas_telecom.py` | `FECHA_LISTA_MAPREX` declarada una sola vez junto a la derivación de la lista |
| `tests/integration/test_auditoria_arenaza.py` | Importa la fecha y las dos rutas de `derivar_listas_telecom` en vez de redefinirlas; ninguna aserción cambió (8/8 antes y después) |
| `tests/unit/test_prediccion.py` | +1 prueba (redondeo de presentación) |
| `tests/unit/test_resultados_ml_multidominio.py` | 4 pruebas de la sección por dominio |
| `docs/plan_pruebas.md` | §10 registro de ejecución (2026‑09‑14) y cifra de cobertura de la fila RNF‑06 |

`docs/calidad_iso25010.md` **no se tocó**: ningún veredicto cambia. Sus celdas de evidencia
(395/395, 98,05 %) son el corte de F.2; el registro vigente está en `plan_pruebas.md` §10.

## Decisiones

| Decisión | Motivo |
|---|---|
| Una base en memoria por acopio | La «lista vigente» de un dominio no debe contaminar el costeo de otro |
| Industrial y sistemas «no evaluables» | Una sola lista real cada uno; fabricar un histórico violaría la restricción de no inventar precios |
| Métricas telecom sobre las 40 partidas, no solo sobre las 8 que cambiaron | La regla no conoce la composición y se aplica a todas; filtrar escondería su limitación |
| Cuantización replicada en `ml/` en vez de importar `formatear_decimal` | `ml/` solo puede importar `core.contracts` (`test_arquitectura.py`) |

## Hallazgos

1. **La regla sin composición falla justo donde el histórico es real.** La variación media de los
   cinco cambios telecom (≈ +25,7 %, de 103,92 / 82,68 en el informe) se aplica a las 40
   partidas; las 32 cuyo PU no cambió quedan a −20,44 % del estimado, apenas fuera del límite
   −20 % de la clase 3. Resultado: 38 advertencias AACE que describen al **estimador**, no a la
   construcción. Para el capítulo V: el contraste AACE solo es interpretable cuando el estimado
   sale de una técnica con composición o con histórico suficiente; con reglas sobre un histórico
   escaso es ruido. Es exactamente la degradación que G2 anticipa, ahora demostrada con datos
   reales. UC‑02 (`actualizar_precios`) sí reproduce los PU exactos porque recorre la
   composición: el módulo predictivo aporta donde la composición no se conoce.
2. **`format(Decimal, ".2f")` redondea al par** (2,125 → 2,12) y la convención de presentación
   del proyecto (`ROUND_HALF_UP`) vive en `core.verification.texto`, fuera de `core.contracts`.
   `ml/` tuvo que replicarla. Insuficiencia menor de contratos: se anota, no se parchea `core/`
   (CLAUDE.md §9). El informe regenerado no cambió (los datos reales no tienen empates); la
   corrección es preventiva.
3. **El manual técnico §7 está desactualizado:** dice que nada en `ml/` importa de `core`,
   «ni siquiera contratos», pero `ml/prediction/reglas.py` importa
   `core.contracts.verificacion` desde I6.3 (lo permite `test_arquitectura.py`). Se corrige en
   M4.2, que tiene el manual en su lista de archivos.
4. **`docs/plan_pruebas.md` §8 (RF → prueba)** no lista las pruebas de integración del sprint
   (`test_presupuesto_arenaza`, `test_auditoria_arenaza`, `test_presupuesto_industrial`,
   `test_presupuesto_sistemas`). Fuera del alcance de M4.1, que limita el cambio al registro de
   ejecución; queda como pendiente.

## Pendientes en archivos vedados por el sprint

- `docs/tesis/plan_redaccion.md` §5 (cifras citables): añadir los conteos 5/40/4/9 y las métricas
  telecom (MAPE 27,83 %, RMSE 28,16 USD, R² 0,8866, 38 AACE) con su comando de regeneración,
  cuando el usuario commitee su sesión de redacción.

## Estado final

- `uv run pytest -W error`: **479 passed in 109,47 s** (345 unitarias + 134 de integración).
  La última edición (rutas de `test_auditoria_arenaza.py`) llegó después de esa corrida: el
  archivo se volvió a ejecutar, 8/8.
- Cobertura: `core/` 98,10 %, `ml/` 99,30 %, `api/` 94,56 %, `adapters/` 88,78 %.
- `uv run ruff check .`: limpio.
- `git diff m-base --stat -- core/`: **vacío**.
- `uv run python scripts/meta_multidominio.py`: **12/12**.

## Compuerta GM4: CRUZADA (informativa)

Criterio del PLAN_MULTIDOMINIO §2: «recuento G2 regenerado por dominio y `resultados_ml.md`
reflejando los dominios poblados». Ambos se cumplen: los cuatro dominios aparecen con su conteo
real y el informe declara la técnica, las métricas donde hay histórico y el motivo donde no lo hay.

## Próxima sesión

**M4.2**: cierre documental del sprint. Incluye los manuales (fuentes de precios y seed por
dominio, y la corrección del §7 del manual técnico), las compuertas GM1–GM4 en
`PLAN_MULTIDOMINIO.md` §2 y la bitácora final.
