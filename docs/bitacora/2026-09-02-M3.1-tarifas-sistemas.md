# Bitácora — Sesión M3.1 (tarifas de sistemas con el tabulador CIV)

**Fecha:** 2026‑09‑02 · **Rama:** `worktree-sprint-multidominio` · **Base:** `85e0eef`
(`feat(industrial): presupuesto de mantenimiento auditado con degradacion gm2 declarada`, cierre
de M2.2) · **Compuerta:** insumo de GM3 (se cruza en M3.2)

## Objetivo

Estructurar las tarifas reales del dominio sistemas —jornales por rol del **tabulador CIV al
01/07/2026**, ya en el repositorio vía MaPreX y estructurado en M0.2
(`referencia_sistemas.csv`, 43 filas)— y componer el APU de un punto de función (horas de cada rol
× tarifa), con **todo supuesto declarado antes del código** y sin inventar ninguna cita: la
productividad HH/PF se marca literalmente como `SUPUESTO (pendiente validacion del autor)` con la
redacción que exige el plan.

**Resultado:** tres roles con tarifa y Ref del tabulador; APU de un punto de función con precio
unitario **110,992365 USD/PF**, igual al calculado a mano; lista canónica de tarifas para UC‑02;
siete pruebas nuevas en verde; `core/` intacto.

## Orden de trabajo (el que pide el plan)

1. **Supuestos primero.** `data/sistemas/fuentes/README.md` documenta (a) la conversión jornal →
   HH con jornada de 8 h, (b) la correspondencia rol ↔ fila del tabulador con filas existentes
   citadas por Ref, (c) la productividad con la frase literal del plan, el reparto de horas por
   rol y la estructura de costos.
2. **RED.** `tests/unit/test_tarifas_sistemas.py` (7 pruebas) escrito antes del fixture; falló
   con `ImportError: cannot import name 'tarifas_sistemas'`.
3. **Cálculo a mano.** Con la fórmula de CLAUDE.md §4, en un script independiente del motor:
   costo directo 87,741 y precio unitario 110,992365 USD/PF. Fijados como regresión exacta.
4. **GREEN.** Fixture escrito; una corrección de la prueba (la frase literal del benchmark queda
   partida en varias líneas en README y docstring: se compara sin saltos de línea); 7/7 en verde.

## Qué se creó

| Archivo | Contenido |
|---|---|
| `data/sistemas/fuentes/README.md` | Fuente y vigencia; (a) jornal → HH; (b) rol ↔ Ref con jornal Bs/USD y tarifa HH; (c) productividad; reparto de horas; estructura de costos; el APU de un PF calculado a mano; qué mejoraría GM3 |
| `data/sistemas/fuentes/lista_tarifas_2026-07.csv` | Lista canónica de UC‑02: los tres roles a su jornal USD (sin unidad: la mano de obra no la lleva en el catálogo) |
| `tests/fixtures/tarifas_sistemas.py` | Única copia: lee `referencia_sistemas.csv` al importarse; `TARIFAS` (rol → `Tarifa` con Ref, descripción literal, jornal Bs/USD, tarifa HH), `HORAS_POR_PF`, `PRODUCTIVIDAD_HH_PF = 8`, `HORAS_JORNADA = 8`, `PARAMETROS_SISTEMAS` (FCAS 0, bono 0), `composicion_para(codigo, descripcion)`, `APU_PUNTO_FUNCION` (`SIS-PF`), `filas_lista_canonica()`; valida al importar que el reparto suma la productividad |
| `tests/unit/test_tarifas_sistemas.py` | Tarifas con Ref del tabulador (tipo, archivo, jornal Bs y USD iguales a la referencia, tarifa HH = jornal/8 cuantizada); productividad en 6–12 y reparto que suma; supuestos declarados en README y docstring; APU con `total_obreros = 1`, costo directo y PU exactos; `cantidad × jornal` ≡ `horas × tarifa_hh` (± redondeo); parámetros sin FCAS ni bono; `composicion_para` conserva las líneas; lista canónica igual al fixture y legible por UC‑02 |

## Las tarifas

| Rol del APU | Ref | Descripción MaPreX (literal) | Jornal (USD) | Tarifa HH (USD) | Horas/PF | Obrero‑día/PF |
|---|---|---|---|---|---|---|
| Analista funcional | `DIS019` | INGENIERO P-5 CIV ANALISTA DIAGNOSTICO | 61,93 | 7,7413 | 1,6 | 0,20 |
| Desarrollador (desarrollo y pruebas) | `DIS042` | INGENIERO COMPUTISTA P-9 CIV (19-20 AÑOS EX | 89,14 | 11,1425 | 6,0 | 0,75 |
| Líder de proyecto | `DIS036` | GERENTE DE PROYECTOS C/CERTIFICACION | 170,00 | 21,2500 | 0,4 | 0,05 |

Costo directo por PF = 0,20 × 61,93 + 0,75 × 89,14 + 0,05 × 170,00 = **87,741**; × 1,15 × 1,10 =
**110,992365 USD/PF**.

## Decisiones

| Decisión | Motivo |
|---|---|
| El catálogo guarda el **jornal**; las horas entran como obrero‑días (`cantidad = horas / 8`) | `LineaManoObra.sueldo` es un jornal por contrato y UC‑02 empareja mano de obra por descripción sin unidad; así `cantidad × jornal` es exactamente `horas × tarifa_hh` (salvo el redondeo de la tarifa, que la prueba acota a 0,001). La tarifa horaria se publica como dato derivado |
| `tarifa_hh = jornal_usd / 8` (no `jornal_bs / 8 / tasa`) | Evita una cuarta copia de la tasa en el repositorio; la diferencia máxima es 0,0001 y queda declarada en el README |
| Tres roles, no los siete de la plantilla | Solo hay fila del tabulador para analista, computista y gerente; QA y arquitecto no existen en el CIV y no se inventan (las horas de pruebas van al desarrollador). Una encuesta TI fechada los aportará por UC‑02 |
| 8 HH/PF | Dentro del rango 6–12 que fija el plan; con jornada de 8 h da un obrero‑día por PF, lo que hace legible la composición (`total_obreros = 1`). Supuesto marcado con la frase literal |
| Reparto 20 / 75 / 5 % | Supuesto del autor; el fixture falla al importar si el reparto no suma la productividad |
| `PARAMETROS_SISTEMAS` con FCAS y bono en cero; administración y utilidad por defecto | El tabulador CIV es de sueldos mínimos profesionales; el 600 % es la convención de la mano de obra de la construcción y el CIV no publica bono (`bono_bs = 0,00` en las 43 filas). Civil e industrial siguen con la estructura completa: el motor recibe un `ParametrosCosto` por presupuesto. Pendiente de la decisión FCAS del dossier G0 |
| Descripciones literales del tabulador, incluida la truncada (`DIS042`) | Mismo criterio que industrial: procedencia evidente en catálogo, informe y lista; M0.2 verificó que el texto faltante no existe en el PDF |
| Un mismo APU para todos los casos de uso (`composicion_para`) | El alcance funcional no aporta datos para diferenciar productividad por módulo; hacerlo sería inventar |

## Hallazgos

1. **La frase literal del plan no cabe en una línea de 100 caracteres**: en README y docstring
   queda partida, y una búsqueda literal falla. La prueba compara con los espacios colapsados. Es
   un detalle de forma, pero conviene recordarlo para cualquier verificador que busque la marca
   `SUPUESTO (pendiente validacion del autor)` seguida del texto.
2. **El contrato representa sin cambios un APU de solo mano de obra con cantidades fraccionarias
   de obrero‑día**: no hizo falta interpretar `rendimiento` como «PF por día de cuadrilla» (la
   otra lectura posible); con `cantidad` en obrero‑días por PF y rendimiento 1, el motor devuelve
   exactamente horas × tarifa. La bitácora I5 sistemas anticipó que no habría insuficiencia de
   contrato; se confirma.
3. **La tasa de conversión ya vive en tres sitios** (`scripts/extraer_maprex.py`,
   `tests/unit/test_referencia_maprex.py`, `scripts/derivar_listas_telecom.py`). Esta sesión evitó
   la cuarta copia derivando de USD; candidato a un módulo único de constantes MaPreX en
   `tests/fixtures/` cuando se toque de nuevo.

## Impedimentos

Ninguno. `core/` intacto.

## Estado final

- `uv run pytest -W error` (sin la prueba de integración de M3.2, que es de la siguiente sesión):
  **468 pruebas en verde** (461 + 7), 0 fallos.
- `uv run ruff check .`: limpio.
- `git diff m-base --stat -- core/`: **vacío**.
- Meta M9: la mitad de fixture (`tests/fixtures/tarifas_sistemas.py`) queda satisfecha; la mitad
  de integración es la Sesión M3.2.

## Pendientes del autor

- Fijar la cita de la productividad HH/PF (ISBSG o literatura del marco teórico) y, si procede,
  ajustar el valor y el reparto por rol.
- Decisión sobre FCAS/bono para honorarios profesionales (dossier G0).
- Encuesta salarial TI fechada (QA, arquitecto, frontend/backend) como lista nueva por UC‑02.

## Próxima sesión

**M3.2** — `scripts/seed_sistemas.py` y `tests/integration/test_presupuesto_sistemas.py`:
adaptador → 9 ítems con la regla IFPUG → presupuesto desde el catálogo (145 PF) → auditoría (R1
reevalúa la regla, R3 acepta `pf`) → UC‑02 con la lista de tarifas; **GM3 CRUZADA**.
