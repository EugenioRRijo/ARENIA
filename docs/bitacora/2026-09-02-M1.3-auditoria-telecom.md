# Bitácora — Sesión M1.3 (auditoría telecom y UC‑02 real)

**Fecha:** 2026‑09‑02 · **Rama:** `worktree-sprint-multidominio` · **Base:** `e4c7764`
(`fix(telecom): marca de ajuste sin colision r6 …`, cierre de M1.2) · **Compuerta:** **GM1
CRUZADA** (ver abajo)

## Objetivo

Cerrar la Fase M1 del PLAN_MULTIDOMINIO: (1) auditar con las siete reglas de verificación
(CLAUDE.md §7) el presupuesto ARENAZA que la Sesión M1.2 reproduce desde SQLite, de modo que la
inconsistencia 80/90 m del tubo corrugado salga del **informe de auditoría con su `origen_id`** y
no viva solo en la documentación; (2) cargar por UC‑02 dos listas de precios telecom reales y
fechadas —los precios ARENAZA (18/05/2026) y la referencia MaPreX de julio 2026 (09/07/2026)— y
obtener el **primer histórico real de `CambioPrecio` fuera del dominio civil**.

**Resultado: GM1 cruzada.** El presupuesto ARENAZA se reproduce del catálogo con diferencia 0,00
(M1.2) y la auditoría emite exactamente un hallazgo R1 ERROR para el tubo corrugado del
presupuesto 2 (`origen_id = Presupuesto_2_ARENAZA.pdf:2:5`, observado 90 m, esperado 80 m,
impacto 33,25 USD). UC‑02 registra cinco `CambioPrecio` fechados el 09/07/2026. `core/` intacto.

## TDD

1. **RED.** `tests/integration/test_auditoria_arenaza.py` (8 pruebas) se escribió primero. Tal
   como M1.2 dejó el catálogo, la aserción (b) fallaba: **ninguna regla existente veía el 80/90**
   (ver «Qué regla lo detecta»).
2. **Implementación en datos, no en núcleo.** `scripts/seed_telecom.py::item_de` declara `regla`
   solo para ese renglón (una línea de datos; `core/` sin tocar). Las dos listas canónicas se
   derivan con un script nuevo.
3. **GREEN.** 8/8 en el archivo de integración; 9/9 en `tests/unit/test_listas_telecom.py`.
4. **La prueba muerde.** Sin la `regla` del ítem, la aserción (b) falla (cero hallazgos con ese
   `origen_id`); con `len(cambios) == 3` (la expectativa de una primera versión de las listas, sin
   el tubo) la aserción (d) falla con 5. La prueba unitaria de listas falló en su primera corrida
   por una expectativa equivocada (7 excluidos en vez de 6: el organizador es un solo insumo con
   dos variantes de catálogo) y se corrigió tras verificar contra el catálogo sembrado.

## Qué se creó o cambió

| Archivo | Contenido |
|---|---|
| `tests/integration/test_auditoria_arenaza.py` | (a) la auditoría corre sobre los dos presupuestos sin alterarlos; (b) exactamente un hallazgo con el `origen_id` del tubo: R1, ERROR, observado 90 / esperado 80, partida `TC-P2-05`; el presupuesto 1 no lo tiene; (c) R5 no reporta ERROR ni CRITICO sin balance declarado (lista vacía); las listas UC‑02 no contienen líneas de ajuste; (d) UC‑02 carga ARENAZA y luego MaPreX y registra cinco `CambioPrecio` con las dos fechas correctas, entre ellos el tubo (99,75 → menor) |
| `scripts/seed_telecom.py` | `REGLA_TUBO_CORRUGADO = "cantidad_computos"` y `item_de` la declara solo para el renglón del hallazgo (excepción documentada en el docstring) |
| `scripts/derivar_listas_telecom.py` | **Nuevo.** Deriva `lista_arenaza.csv` (27 filas) desde las composiciones de M1.2 y `lista_maprex_2026-07.csv` (5 filas) desde `referencia_telecom.csv`; `CORRESPONDENCIA` es la única tabla ARENAZA ↔ MaPreX; `--verificar` compara con los CSV en disco; imprime el contraste. No escribe ningún precio propio |
| `data/telecom/fuentes/lista_arenaza.csv`, `lista_maprex_2026-07.csv` | Las dos listas canónicas de UC‑02 (`tipo,insumo,unidad,precio`) |
| `data/telecom/fuentes/README.md` | Sección «Listas de precios canónicas para UC‑02»: vigencias, criterio de la lista 1 con sus exclusiones, correspondencia de la lista 2 con la tabla de contraste y la tabla de lo que queda fuera y por qué |
| `tests/unit/test_listas_telecom.py` | Los CSV en disco son exactamente los derivados; `--verificar` pasa; UC‑02 los lee; los cuatro casos de exclusión; el tubo entra a 99,75 por tubo; la lista 2 sale de bolívares con la tasa de M0.2 y, sin conversión, reproduce el `precio_usd` de M0.2; toda fila de la lista 2 tiene su par en la lista 1 y lo cambia |

## Qué regla detecta el 80/90 y por qué hizo falta una línea de datos

La Sesión M1.2 dejó las dos cantidades del tubo en `ItemComputo.parametros`
(`cantidad_computos = 80`, `cantidad_presupuesto = 90`) pero **sin `regla`**: «la cantidad se
transcribe, no se deriva». Con eso, ninguna regla existente lo veía:

- **R1** (`TrazabilidadGeometrica`) solo compara `regla` evaluada contra `cantidad`; un ítem
  TABULAR sin regla no genera hallazgo.
- **R7** (`ConciliacionPresupuestoPlan`) solo actúa si el presupuesto declara plan de trabajo, y
  ARENAZA no declara ninguno.

La brecha se cierra **por el lado de datos**: el renglón del tubo del presupuesto 2 declara
`regla = "cantidad_computos"`, una referencia directa al parámetro que ya viajaba con el ítem. R1
no exige que la regla sea una fórmula geométrica: evalúa lo declarado con los `parametros`
declarados y lo compara con `cantidad`. Así compara 90 (impreso en «Presupuesto», la cantidad que
vale) contra 80 (impreso en «Computos metricos») y emite el hallazgo con el `origen_id` de la fila
del PDF. **Ningún archivo de `core/` cambió**: la regla ya existía y ya hacía exactamente esa
comparación para cualquier dominio. El presupuesto 1 no declara regla (su tubo trae 90 m en las
dos tablas) y R1 no dice nada de él, que es lo correcto.

## Qué reporta cada regla (los dos presupuestos)

| Regla | TC-001 | TC-002 | Por qué |
|---|---|---|---|
| R1 Trazabilidad | — | **ERROR** `TC-P2-05` | La única cantidad con regla declarada; observado 90 m, esperado 80 m, impacto 33,25 USD (10 m × 3,325) |
| R2 Cierre de curva | INFO | INFO | «el presupuesto no declara curva de inversion: no hay cierre que verificar» (RF‑23: sin datos no se finge error) |
| R3 Coherencia dimensional | — | — | La unidad del ítem y la del APU salen del mismo renglón: coinciden por construcción |
| R4 Especificaciones | — | — | Los ítems solo declaran `_unidad_original` (decisión de M1.2 para no sembrar INFO falsos) |
| R5 Balance volumétrico | — (lista vacía) | — (lista vacía) | Ningún ítem declara la directiva `_balance`; «sin datos» se realiza como lista vacía, igual que fija `tests/unit/test_verification.py` para civil sin balance. Lo que RF‑23 garantiza —nunca ERROR ni CRITICO por falta de datos— queda probado |
| R6 Depreciación | — | — | Un solo equipo (guaya guía) con el mismo factor 1,00 en los dos APU: uniforme y, por tanto, sin hallazgo. Que la herramienta se impute al 100 % es una observación documental, no una inconsistencia para R6 |
| R7 Conciliación con el plan | — | — | Sin plan de trabajo declarado |

Informe de TC-002: 2 hallazgos, resultado **NO CUMPLE**; TC-001: 1 hallazgo INFO, **CUMPLE**.

## UC‑02: las dos listas y el primer contraste de mercado

**Lista 1 (`lista_arenaza.csv`, 18/05/2026, 27 filas).** Entra todo insumo del catálogo al que la
fuente le da un único precio unitario no contradicho. El script decide las exclusiones a partir
de los datos, no de nombres: las dos líneas de ajuste (obligación de M1.2), la suma global
«Micelaneos», los dos renglones cuyo total impreso contradice cantidad × precio unitario (cámara
bullet y jack coupler: el PDF les da dos precios incompatibles) y el organizador (18,00 y 19,00
en la misma fuente; UC‑02 aplicaría una fila a las dos variantes y fabricaría un cambio). El
tubo corrugado **sí entra**, a 99,75 por tubo de 30 m, bajo las dos descripciones del catálogo.

**Lista 2 (`lista_maprex_2026-07.csv`, 09/07/2026, 5 filas).** Criterio de dos condiciones:
(a) la nota de M0.2 declara la fila MaPreX equivalente del insumo ARENAZA sin salvedad de
atributo; (b) misma unidad de venta, o factor de conversión impreso por la propia fuente ARENAZA.

| Insumo | Ref MaPreX | Factor | ARENAZA | MaPreX | Variación |
|---|---|---|---|---|---|
| Anillo E.m.t. 2 | `ELE033` | 1 | 3,96 | 9,5364 | +140,8 % |
| Toma Doble Con Tierra 270 20a | `ELA190` | 1 | 5,05 | 6,7947 | +34,5 % |
| Cajetin Plástico 4x2 Pvc | `ELE654` | 1 | 2,00 | 1,9073 | −4,6 % |
| Tubo Corrugado Flexible 1" (tubo de 30 m), P1 y P2 | `ELE906` | 30 | 99,75 | 78,6751 | −21,1 % |

El tubo es la única conversión admitida: el factor 30 lo imprime el PDF («30 MTS», «3\*99,75»),
y es el insumo del hallazgo 80/90 y el que M1.2 dejó explícitamente para este contraste. Se
convierte desde bolívares (49 830,00 Bs → 78,6751 USD); multiplicar el USD ya redondeado daría
78,6750. Cinco cambios para cuatro correspondencias porque el catálogo guarda el tubo bajo dos
descripciones (los PDF lo imprimen distinto).

**Lectura del contraste, con cautela.** Son fechas distintas (18/05 vs 09/07/2026), canales
distintos (distribuidor vs referencia nacional en Bs convertida a la tasa del 01/07) y cuatro
insumos: no es una conclusión de mercado, es el primer histórico real con el que el módulo
predictivo (que sigue siendo de reglas, G2) tiene algo que mirar fuera de civil.

## Decisiones

| Decisión | Motivo |
|---|---|
| Declarar `regla` en el ítem en vez de crear una regla nueva | El PLAN lo exige («usar la que las reglas existentes dicten SIN modificarlas»). R1 ya compara cualquier regla declarada; el contrato `ItemComputo` (regla + parámetros) bastó |
| La regla es la referencia al parámetro, no una fórmula | Es la forma más honesta de decir «esta cantidad debe coincidir con la de Computos metricos»; inventar una expresión geométrica habría sido ficción |
| Un script para derivar las dos listas, con `--verificar` y prueba de igualdad | Mismo criterio que `extraer_arenaza.py`: la correspondencia ARENAZA ↔ MaPreX vive en un solo lugar y los CSV se pueden regenerar sin transcribir precios. Las exclusiones de la lista 1 se calculan de los datos |
| Incluir el tubo en las dos listas, con conversión declarada | Excluirlo habría perdido el único contraste que M1.2 preparó a propósito; el factor no es un supuesto, lo imprime el PDF |
| No admitir las conversiones de empaque (bolsa ×100, bobina ×300) | M0.2 las anota como salvedad y el PDF no imprime su aritmética. Quedan en el README como convertibles si el autor acepta el supuesto |
| Lista 2 convertida desde bolívares, no desde el `precio_usd` de M0.2 | Multiplicar un USD ya cuantizado amplifica su redondeo (× 30 → 78,6750 vs 78,6751); `_verificar_tasa` comprueba en cada corrida que la tasa es la de M0.2 |
| `_referencia_por_ref` tolera Refs repetidas entre listados | `EFO001` es fibra en `materiales.pdf` y fusionadora en `equipos.pdf` en el CSV de M0.2; se exige unicidad solo en las Refs usadas |

## Hallazgos

1. **Una discrepancia entre dos tablas de la misma fuente es invisible para las siete reglas si
   no se declara como regla.** R1 cubre el caso solo cuando el segundo valor se expresa como
   `regla` del ítem; una cantidad transcrita sin regla no se contrasta con nada. Los contratos
   bastaron para expresarlo (no es una insuficiencia), pero es una observación para el capítulo de
   resultados: la trazabilidad exige declarar de dónde debería salir cada cantidad, incluso cuando
   la fuente la imprime dos veces.
2. **R6 no ve la imputación al 100 % de una herramienta** si el factor es uniforme: su definición
   es de consistencia entre APU y de criterio declarado, no de razonabilidad del factor. La guaya
   guía queda como observación documental (M1.2, mecanismo 4).
3. **El catálogo duplica el tubo** porque los PDF lo describen distinto en P1 y P2. Es fiel a la
   fuente, pero la normalización semántica de I2 (`ml/normalization`) es la candidata a
   detectarlo como el mismo insumo; no se hizo aquí (fuera de alcance).
4. **`ruff format --check` sigue sin estar limpio en la base del sprint** (hallazgo 3 de M1.2);
   no se corrió `ruff format` por la misma razón (tocaría `core/`).

## Impedimentos

Ninguno. No hizo falta tocar `core/`.

## Estado final

- `uv run pytest -W error`: **448 pruebas en verde** (431 al cierre de M1.2 + 8 de auditoría +
  9 de listas), 0 fallos.
- `uv run ruff check .`: limpio.
- `git diff m-base --stat -- core/`: **vacío**.
- `uv run python scripts/meta_multidominio.py`: **10/12** — **M7 pasa a OK** («Auditoria telecom
  detecta el hallazgo 80/90: 8 pasadas / 0 fallidas»); pendientes M8 (M2.1) y M9 (M3.1).
- `uv run python scripts/derivar_listas_telecom.py --verificar`: «Los dos CSV coinciden con lo
  derivado».

## Compuerta GM1: CRUZADA

Criterio del PLAN_MULTIDOMINIO §2: «el presupuesto ARENAZA se reproduce del catálogo ± 0,01 y la
auditoría detecta la inconsistencia 80/90 m». Reproducción exacta (M1.2, diferencia 0,00 en los
dos presupuestos) y hallazgo R1 ERROR con `origen_id` (esta sesión). Sin degradación: los datos
estaban en el repositorio. Criterio de cierre de la sesión: 80/90 detectado ✓; UC‑02 telecom en
verde ✓ (cinco `CambioPrecio` reales).

## Pendientes del autor

- Decidir si admite las conversiones de empaque (conectores RJ45 ×100, bobinas UTP ×300/305)
  como correspondencias adicionales de la lista 2; el README las deja anotadas como convertibles.
- Confirmar físicamente las tres equivalencias «sin salvedad» (anillo, tomacorriente, cajetín).
- Lista 3 del PLAN (cotizaciones de distribuidores telecom) si llegan del trabajo de campo.

## Próxima sesión

**M2.1** — catálogo industrial con la referencia MaPreX declarada como degradación de GM2
(`referencia_industrial.csv` de M0.2). También puede abrirse **M3.1** (tarifas sistemas con el
tabulador CIV) sin depender de M2.
