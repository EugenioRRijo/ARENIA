# Bitácora — Dossier de G0 e instrumento de RNF‑04

**Fecha:** 2026‑08‑31 · **Rama:** `inc/G0-dossier` (fusionada a `main` con `--no-ff`) ·
**Base:** `747e68b` (UC‑08 en API y UI) · **Origen:** siguiente paso tras completar el PLAN
(«continúa»): G0 es el primer pendiente y su reunión necesitaba un paquete único.

## Qué se hizo

**[docs/dossier_g0.md](../dossier_g0.md)**: el paquete completo para la reunión del tutor.

1. **Inventario de decisiones D1–D8**, reconstruido barriendo todas las bitácoras (`grep G0|tutor`):
   R7 (con el hallazgo adicional 9 como evidencia), la regla candidata R8 (octavo defecto del
   caso, `ResumenCarga.variantes`), OE7 vs resultado complementario (esqueleto §3), fuente
   primaria del FCAS, flujo de creación de partidas (RF‑16, el hallazgo más recurrente),
   postcondición de UC‑08 en memoria, alcance de M9 y la terminología BIM‑5D/«sombra digital».
   Cada una con contexto, dónde está documentada, impacto si se rechaza y recomendación.
2. **Salvedades informativas** separadas de las decisiones: G1 (con el pedido concreto: exports
   IFC 4 de dos herramientas reales), G2 (limitación declarada), RNF‑07 (Linux) y la
   inconsistencia interna de la fuente ARENAZA (80 vs 90 m, candidata a caso R7 telecom).
3. **Anexo A: instrumento de RNF‑04** (juicio de expertos): procedimiento (informe sin editar,
   sin explicación previa — eso es lo que se mide), 8 afirmaciones Likert 1–5 alineadas con lo
   que el informe promete (severidad, impacto cuantificado, `origen_id`, orden, lenguaje,
   accionabilidad, confianza) y campo abierto; registro definido (promedios, n, versión).
4. Índice de docs con la fila nueva; `calidad_iso25010.md` §4 enlaza el instrumento.

## Decisiones

| Decisión | Motivo |
|---|---|
| Un solo documento con D1–D8 numeradas y recomendación explícita en cada una | Las decisiones estaban dispersas en once bitácoras; una reunión de G0 sin agenda cerrada las habría perdido. La recomendación se separa del pedido para que el tutor pueda rechazarla sin ambigüedad |
| El dossier no repite evidencia: cifras en §2 y todo lo demás enlazado | DRY documental; el dossier caduca con la reunión, los documentos que enlaza no |
| El instrumento de RNF‑04 viaja dentro del dossier y no como documento propio | Su aplicación depende de la misma reunión (población y fecha las acuerda el tutor); si G0 lo modifica, se promueve a documento propio con los resultados |
| Recomendaciones: aprobar R7 y R8, opción OE7, validar UC‑08 en memoria y M9 | En los cuatro casos la evidencia empírica ya existe; recomendar lo contrario sería pedirle al tutor que descarte trabajo verificado sin ganancia |

## Estado al cierre

```
uv run pytest -W error  -> 395 passed en 59,6 s (sesion solo de documentos)
uv run ruff check .     -> limpio
git diff --stat core/   -> vacio
```

## Próximos pasos

1. **Reunión de G0** con este dossier; volcar las decisiones D1–D8 a la ERS/bitácora según salgan.
2. Si D2 se aprueba: sesión corta para la regla R8 (mismo patrón TDD que R1–R7).
3. **Capítulos de la tesis** sobre `docs/tesis/esqueleto_tesis.md` — todos los indicadores ya
   están medidos; es el siguiente paso que no depende de nadie externo.
4. G1 (exports reales), RNF‑04 (aplicar el anexo A), RNF‑07 (Linux) — con dueño externo.
