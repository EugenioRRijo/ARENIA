# Bitácora — Sprint 0 (esqueleto y bases)

**Fecha:** 2026‑08‑26 · **Rama:** `main` · **Compuertas cruzadas:** ninguna (G0 pendiente de revisión del tutor)

## Objetivo del sprint

Dejar el repositorio con su constitución (`CLAUDE.md`), la metodología Scrum‑Cascada + DRY documentada,
la estructura de paquetes, los contratos estables, la línea base ejecutable y los esqueletos de los
documentos de Fase 0, de modo que la Sesión 0.1 del PLAN pueda empezar sin reconstruir contexto.

## Qué se hizo

| Commit | Contenido |
|---|---|
| `chore: inicializa repositorio y constitucion del proyecto` | `.gitignore`, `pyproject.toml` (uv, ruff, pytest), `README.md`, `CLAUDE.md` |
| `docs(metodologia): scrum-cascada y principio DRY` | `docs/metodologia.md`, `docs/README.md`, copias de las fuentes en `docs/fuentes/` |
| `feat(core): contratos de interfaz y estructura del proyecto` | árbol de paquetes con docstrings, `core/contracts/*`, `test_contracts.py`, `test_arquitectura.py` |
| `test(core): fixtures de linea base y pruebas del motor` | `tests/fixtures/apu_linea_base.py`, `conftest.py`, `test_linea_base.py`, `test_costing.py` (rojo), `data/` |
| `docs: esqueletos ERS, modelo de datos, arquitectura y tesis` | `docs/ERS.md`, `docs/modelo_datos.md`, `docs/arquitectura.md`, `docs/linea_base.md`, `docs/tesis/esqueleto_tesis.md`, esta bitácora |

Estado de la suite al cierre: **88 pruebas en verde, 7 en rojo por `ImportError`** en `test_costing.py`
(esperado hasta I0.3). `ruff check` y `ruff format --check` limpios.

## Decisiones

| Decisión | Motivo |
|---|---|
| Raíz del repositorio en `TESIS/` (no en `apu_system/`) | es la carpeta donde se abre Claude Code; `PLAN_DESARROLLO.md` queda dentro del repo |
| `uv` como gestor de entorno; extras por capa (`civil`, `ml`, `ui`, `api`) | instalado en la máquina; evita instalar ifcopenshell/xgboost antes de necesitarlos |
| `CLAUDE.md` reconstruido desde las Bases del anteproyecto y el PDF de los APU | el original referido por el PLAN no existe en ninguna carpeta |
| Códigos de partida `LB-xx` en la línea base | los códigos COVENIN se asignan al cargar el catálogo (I0.4); no se inventan |
| Los contratos incluyen `Presupuesto`, `PartidaPresupuestada`, `PuntoCurva` y `Hallazgo` | las reglas de verificación necesitan una forma estable en memoria; los modelos SQLAlchemy mapearán hacia ella |
| `ItemComputo.especificaciones` (mapa texto→texto) | sin él la regla R4 (diámetro 3/4" vs 4") no tendría dato que comparar |
| Séptima regla R7 `ConciliacionPresupuestoPlan` | las Bases listan cinco reglas para siete hallazgos; el PDF muestra que los montos diarios no coinciden con las partidas. **Validar con el tutor en I4** |
| `test_costing.py` importa el motor dentro de un helper | así el resto de la suite corre en verde mientras el motor no existe; las pruebas no se tocarán en I0.3 |
| `test_arquitectura.py` como prueba permanente | convierte la hipótesis central en una comprobación automática, complementaria a `git diff --stat core/` |
| Alias de unidades que NO igualan "pieza" con "m" ni "m2" con "m3" | la regla R3 debe seguir detectando los hallazgos 4 y 8 |

## Hallazgos de la investigación

1. **La fórmula del APU se reprodujo al centavo** en los cinco casos: materiales sin dividir entre
   rendimiento; equipos y mano de obra sí; prestaciones 600 % sobre sueldos; bono de 1,00 por obrero;
   15 % y 10 % en cascada; sin redondeos intermedios. Está fijada en CLAUDE.md §4.
2. **Hallazgo 7 localizado:** el "Vehículo de transporte" lleva depreciación 0,03 en el APU de tubería
   y 1,00 en los otros cuatro.
3. **Dos inconsistencias nuevas** (8 y 9 en `docs/linea_base.md`): el encofrado está en "m3" en cómputo
   y presupuesto pero en "m2" en el APU; y cada monto diario del plan de trabajo es menor que el de su
   partida (raíz del hallazgo 3).
4. El APU de tubería usa un "Maestro Electricista" en una partida de plomería: dato para I2.
5. Los presupuestos ARENAZA (dominio telecom, `data/samples/telecom/`) no usan APU por rendimiento:
   la mano de obra se estima como "50 % del total". **Decisión pendiente para I5:** cómo representa un
   adaptador una política de mano de obra distinta sin tocar `core/` (¿`ParametrosCosto` por dominio?
   ¿una `ComposicionAPU` con mano de obra proporcional? ¿es un hallazgo de insuficiencia del contrato?).
6. El PLAN amplía la tesis a multidominio, pero las Bases tienen seis objetivos solo sanitarios.
   Opciones en `docs/tesis/esqueleto_tesis.md` §3; decidir con el tutor antes del anteproyecto formal.

## Impedimentos y riesgos

- `data/samples/tanquilla.ifc` no existe: hay que modelarla antes de I3.1 (Definition of Ready).
- El valor del FCAS (600 %) proviene del APU auditado; confirmar con fuente primaria (convención colectiva
  vigente, guías del CIV) antes de la ERS definitiva.
- `.venv` dentro de OneDrive: sincroniza miles de archivos; alternativa en README.
- `uv sync` instaló pandas 3.0.5 y SQLAlchemy 2.0.52; revisar compatibilidad de openpyxl/streamlit al llegar a I0.5/I1.

## Próxima sesión (Definition of Ready)

- [ ] Sesión 0.1 — ERS: partir de `docs/ERS.md`, derivar ≥ 20 RF con trazabilidad a los UC.
- [ ] Llevar al tutor: R7, OE7 vs resultado complementario, fuente primaria del FCAS.
- [ ] Gestionar fuente de datos (MaPreX o trabajos de grado con APU en anexos).
